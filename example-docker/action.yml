name: 'Example Docker Action'
description: 'A simple example Docker action that demonstrates using a local Dockerfile as a GitHub Action'
author: 'GitHub Action Experiments'

inputs:
  message:
    description: 'Optional custom message to display'
    required: false
    default: 'Hello from Docker Action!'

runs:
  using: 'docker'
  image: 'Dockerfile'
